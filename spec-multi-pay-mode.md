# 多计薪模式 — 详细设计稿

## 一、数据模型变更

### 1.1 Project 新增字段

```js
// 当前字段保留不变，新增以下字段：

pay_mode: 'hourly'      // 计薪方式: hourly | daily | piece
daily_rate: 0           // 日薪（日薪模式使用，单位：元/天）
piece_rate: 0           // 计件单价（计件模式使用，单位：元/件）
piece_unit: '件'        // 计件单位（件/㎡/m³/吨，可自定义）
```

### 1.2 Record 新增字段

```js
// 当前字段保留不变，新增以下字段：

pay_mode: 'hourly'      // 冗余字段，记录时确定的计薪方式
days: 1                 // 日薪模式：工作天数
quantity: 0             // 计件模式：完成数量
piece_rate: 0           // 计件模式：当时的单价（冗余，防止价格变更后历史记录出错）
daily_rate: 0           // 日薪模式：当时的日薪（冗余，同上）
```

### 1.3 计算逻辑核心公式

```
// 时薪模式（保留现有）
加班费 = 工时 × 时薪
例：3.5h × ¥25/h = ¥87.5

// 日薪模式（新增）
加班费 = 天数 × 日薪
例：1天 × ¥300/天 = ¥300

// 计件模式（新增）
加班费 = 数量 × 单价
例：50件 × ¥6/件 = ¥300
```

### 1.4 全局薪资设置变化

当前薪资设置页只有时薪相关配置。需要调整为：

```
┌────────────────────────────────┐
│  加班费设置                      │
│                                 │
│  ● 时薪制（当前已有）              │
│  ○ 日薪制（新增）                 │
│  ○ 计件制（新增）                 │
│                                 │
│  [显示对应模式的全局默认值]        │
└────────────────────────────────┘
```

注意：全局薪资设置仅作为"默认值"，项目级别的计薪方式会覆盖全局设置。如果用户没有启用项目,则使用全局薪资设置的计薪方式和价格。

---

## 二、项目编辑页（project-edit.vue）— UI 改动最大

### 2.1 页面布局（从上到下）

```
┌─────────────────────────────────────┐
│  ← 新建项目                         │  ← NavBar
├─────────────────────────────────────┤
│                                      │
│  项目名称                            │
│  ┌──────────────────────────────┐   │
│  │  例如：xx工地                  │   │
│  └──────────────────────────────┘   │
│                                      │
│  ── 计薪方式 ──                      │
│                                      │
│  ┌────┬────┬────┐                   │
│  │ ⏱  │ 📅 │ 📦 │                   │
│  │时薪 │日薪 │计件 │                   │
│  ├────┴────┴────┤                   │
│  │ 选时薪：                       │   │
│  │ 平日 ¥ [25 ] /h               │   │
│  │ 周末 ¥ [32.5] /h              │   │
│  │ 节假日 ¥ [50 ] /h             │   │
│  └──────────────────────────────┘   │
│                                      │
│  （或者展开日薪模式👇）               │
│  ┌──────────────────────────────┐   │
│  │ ⏱ 时薪 │ 📅 ●日薪 │ 📦 计件  │   │
│  ├──────────────────────────────┤   │
│  │ 日薪标准                       │   │
│  │ ¥ [ 300 ] / 天               │   │
│  └──────────────────────────────┘   │
│                                      │
│  （或者展开计件模式👇）               │
│  ┌──────────────────────────────┐   │
│  │ ⏱ 时薪 │ 📅 日薪 │ 📦 ●计件  │   │
│  ├──────────────────────────────┤   │
│  │ 单价 ¥ [ 6 ] / [ 件  ▼]     │   │
│  │ 单位可选：件/㎡/m³/吨         │   │
│  └──────────────────────────────┘   │
│                                      │
│  颜色                                │
│  ○ ○ ● ○ ○ ○ ○ ○                    │
│                                      │
│  [保存]                             │
└─────────────────────────────────────┘
```

### 2.2 交互说明

- **三个计薪方式用卡片式 tab 切换**，选中态高亮，未选中态灰色
- **切换时下方表单区域联动变更**，只显示当前模式相关的输入
- 每个模式下方显示示例说明，降低用户理解成本

---

## 三、记工时页（record.vue）— 按模式自适应

### 3.1 时薪模式

当前已有的逻辑，保持不变。只是当项目选了"日薪"或"计件"模式时，隐藏"快捷时长"和"起止时间"。

### 3.2 日薪模式

```
┌─────────────────────────────────────┐
│  ← 记录加班                         │  ← NavBar
├─────────────────────────────────────┤
│                                      │
│  日期：2026-05-07 周四               │
│                                      │
│  项目：xx工地（日薪 ¥300/天）        │
│                                      │
│  ─────────────────────────────────   │
│                                      │
│  ┌──────────────────────────────┐   │
│  │                              │   │
│  │          ⏱ 上班打卡          │   │
│  │        [  大按钮  ]          │   │
│  │                              │   │
│  │   今日已记：10 天             │   │
│  │   本月累计 ¥3,000            │   │
│  │                              │   │
│  └──────────────────────────────┘   │
│                                      │
│  加班说明（选填）                     │
│  ┌──────────────────────────────┐   │
│  │  正常施工 / 加班赶工 / ...    │   │
│  └──────────────────────────────┘   │
│                                      │
│  也可手动修改天数                     │
│  天数：[ 1  ] 天                     │
│  加班费：¥300                        │
│                                      │
│  [保存]                             │
└─────────────────────────────────────┘
```

核心思路：**日薪模式的最常用操作是 "今天干了，记一笔"**，所以大按钮是核心交互。点击大按钮直接记录当天，数量为1天。

### 3.3 计件模式

```
┌─────────────────────────────────────┐
│  ← 记录加班                         │
├─────────────────────────────────────┤
│                                      │
│  日期：2026-05-07 周四               │
│                                      │
│  项目：xx工厂（计件 ¥6/件）          │
│                                      │
│  ─────────────────────────────────   │
│                                      │
│  今天做了多少？                       │
│                                      │
│  ┌─────┬─────┬─────┬─────┐          │
│  │ -10 │ -1  │ [+1]│ +10 │          │  ← 快速加减
│  └─────┴─────┴─────┴─────┘          │
│                                      │
│           [  50  ]  件               │  ← 大号数字
│                                      │
│  加班费：¥300                        │
│                                      │
│  备注（选填）                         │
│  ┌──────────────────────────────┐   │
│  │  搬运货物                      │   │
│  └──────────────────────────────┘   │
│                                      │
│  [保存]                             │
└─────────────────────────────────────┘
```

核心思路：**计件模式的核心是快速增减数量**，用大号加减按钮 + 醒目数字。

---

## 四、记账主页（index.vue）— 兼容适配

### 4.1 摘要卡片

```
┌──────────────────────────────┐
│  本月收入                      │
│  48.5 小时          ¥1,860   │  ← 时薪项目显示小时
│  22 天              ¥6,600   │  ← 日薪项目显示天数
│  200 件             ¥1,200   │  ← 计件项目显示件数
│  ──────────────────────────  │
│  合计 ¥9,660                 │
└──────────────────────────────┘
```

### 4.2 日历点标记

- 日历上的小圆点保持不变（有记录就显示）
- 点击日期弹出日汇总时，每条记录显示对应的计薪方式和金额

### 4.3 记录列表

每条记录行尾部显示模式标签，方便识别：

```
05-07  19:00-22:30  3.5h  ¥87.5    [时薪]
05-07  xx工地       1天   ¥300     [日薪]
05-07  xx工厂       50件  ¥300     [计件]
```

---

## 五、对账页（recon.vue）— 统一聚合

### 5.1 对账卡片

```
┌──────────────────────────────┐
│  5月对账                      │
│                              │
│  预估总收入   ¥9,660          │
│                              │
│  实发金额  ¥[              ]  │  ← 用户填工资条上的数
│                              │
│  ──────────────────────────  │
│  差额    -¥340  🔴 少发了    │  ← 负数红色，正数绿色
└──────────────────────────────┘
```

### 5.2 明细列表（按项目分组）

```
xx工地（日薪 ¥300/天）
  05-01  1天  ¥300   ✅ 已结算           │
  05-02  1天  ¥300   ✅ 已结算           │  可折叠
  05-03  1天  ¥300   ⏳ 未结算           │
  合计：22天  ¥6,600                     │

麦当劳兼职（时薪 ¥25/h）  
  05-01  19:00-22:30  3.5h  ¥87.5  ⏳
  05-02  18:00-23:00  5h    ¥125   ⏳
  合计：48.5h  ¥1,212

xx工厂（计件 ¥6/件）
  05-05  50件  ¥300  ⏳
  05-06  60件  ¥360  ⏳
  合计：110件  ¥660
```

---

## 六、统计分析页（stats.vue）— 兼容

### 6.1 统计摘要

```
┌──────┬──────┬──────┐
│ 总工时 │ 总收入 │ 记录数 │
│  48.5h │ ¥9,660│  45   │
├──────┼──────┼──────┤
│ 总天数 │ 总件数 │       │
│  22天  │ 200件 │       │
└──────┴──────┴──────┘
```

注意：时薪/日薪/计件三种模式的统计数值维度不同，摘要卡片上同时显示"总工时"和"总天数"和"总件数"，取有值的显示。

### 6.2 趋势图

- 趋势图使用金额（¥）作为统一维度，因为不管哪种模式最终都算成钱
- 月份对比使用"总收入（元）"

---

## 七、薪资设置页（salary.vue）— 默认值变更

当前页面的"三个时薪输入"改为"三种计薪方式的默认值"，结构如下：

```
┌──────────────────────────────┐
│  加班费设置                    │
│                              │
│  默认计薪方式                  │
│  ○ 时薪制  ● 日薪制  ○ 计件制 │
│                              │
│  ── 日薪默认值 ──            │
│  ¥ [ 300 ] / 天              │
│                              │
│  ── 时薪默认值（折叠）──      │
│  平日 ¥ [ 25 ] /h            │
│  周末 ¥ [ 32.5 ] /h          │
│  节假日 ¥ [ 50 ] /h          │
│                              │
│  精度                         │
│  15分钟                       │
└──────────────────────────────┘
```

说明：默认计薪方式影响"未绑定项目的记录"使用哪种模式计算。

---

## 八、项目管理页（projects.vue）— 卡片适配

当前项目卡片显示：

```
┌──────────────────────────────┐
│ 🟩 xx工地                     │
│  平日¥25  周末¥32.5  节¥50   │
│                     [删除]   │
└──────────────────────────────┘
```

改为：

```
┌──────────────────────────────┐
│ 🟩 xx工地                     │
│  📅 日薪 ¥300/天   ·  已记22天│
│                     [删除]   │
├──────────────────────────────┤
│ 🟦 麦当劳兼职                  │
│  ⏱ 时薪 ¥25/h     ·  已记48h │
│                     [删除]   │
├──────────────────────────────┤
│ 🟪 xx工厂                     │
│  📦 计件 ¥6/件     ·  已记200件│
│                     [删除]   │
└──────────────────────────────┘
```

每张卡片左上角显示计薪方式图标，中间显示主要单价，右侧显示累计量。

---

## 九、数据库 Schema 变更

### 9.1 project-config 集合

```json
{
  "_id": "xxx",
  "user_id": "xxx",
  "name": "xx工地",
  "color": "#1B8A5A",
  "sort_order": 0,
  "pay_mode": "daily",           // 新增
  "daily_rate": 300,              // 新增
  "piece_rate": 0,               // 新增
  "piece_unit": "件",            // 新增
  "weekday_rate": 25,            // 保留
  "weekend_rate": 32.5,          // 保留
  "holiday_rate": 50,            // 保留（时薪模式用）
  "is_archived": false,
  "created_at": 1234567890,
  "updated_at": 1234567890
}
```

### 9.2 overtime-record 集合

```json
{
  "_id": "xxx",
  "user_id": "xxx",
  "date": "2026-05-07",
  "pay_mode": "daily",           // 新增
  "days": 1,                      // 新增（日薪模式用）
  "quantity": 0,                 // 新增（计件模式用）
  "piece_rate": 0,               // 新增（计件模式用）
  "daily_rate": 300,             // 新增（日薪模式用）
  "duration": 0,                 // 时薪模式用
  "start_time": "",              // 时薪模式用
  "end_time": "",                // 时薪模式用
  "overtime_type": "weekday",    // 时薪模式用
  "project_id": "xxx",
  "rate": 300,
  "pay": 300,
  "subsidies": { "night_shift": 0, "meal": 0, "transport": 0 },
  "deduction": { "amount": 0, "note": "" },
  "settled": false,
  "remark": "正常施工",
  "created_at": 1234567890,
  "updated_at": 1234567890
}
```

---

## 十、迁移策略

### 10.1 存量数据

- 已有的项目：`pay_mode` 默认设为 `'hourly'`，`daily_rate` 和 `piece_rate` 默认为 0
- 已有的记录：`pay_mode` 默认设为 `'hourly'`，`days` 默认为 1，`quantity` 默认为 0

### 10.2 兼容逻辑

```js
// 计算时，根据 pay_mode 分支
function calculatePay(record, rate) {
  switch (record.pay_mode) {
    case 'daily':
      return (record.days || 1) * (record.daily_rate || rate || 0)
    case 'piece':
      return (record.quantity || 0) * (record.piece_rate || rate || 0)
    case 'hourly':
    default:
      return (record.duration || 0) * rate
  }
}
```

### 10.3 项目创建默认值

新建项目时，第一个选项是"选择计薪方式"，未选时无法进入下一步。选择后显示对应模式的单价输入。

---

## 十一、实现顺序

| 优先级 | 任务 | 文件 | 工时 |
|--------|------|------|------|
| P0 | 数据模型扩展（字段+Store） | constants.js, projectStore.js, overtimeStore.js | ~1h |
| P0 | 项目编辑页 — 计薪方式选择 UI | project-edit.vue | ~2h |
| P1 | 记工时页 — 日薪/计件模式 UI | record.vue | ~3h |
| P1 | 计算逻辑分支（calculatePay） | overtimeStore.js + 云函数 | ~1h |
| P2 | 首页摘要卡片 — 多模式兼容 | index.vue | ~1h |
| P2 | 对账页 — 项目分组+模式标签 | recon.vue | ~2h |
| P3 | 项目管理页 — 卡片样式适配 | projects.vue | ~1h |
| P3 | 薪资设置页 — 默认计薪方式 | salary.vue | ~1h |
| P3 | 统计分析页 — 多维度统计 | stats.vue | ~1h |
| P4 | 云函数 Schema 扩展 | overtime-calc/index.js | ~1h |

---

## 十二、常见用户场景

### 场景 1：建筑工老王

```
老王在 xx工地 干活，日薪 ¥300。

每天下班：
  打开 App → 首页 → 点"记一笔"
  选择项目"xx工地" → 自动识别为日薪模式
  点一下"上班打卡"大按钮 → 保存
  ✅ 完成，耗时 3 秒

月底：
  打开对账页 → 看到"xx工地 5月 22天 ¥6,600"
  老板说只干了 20 天 → 打开明细给老板看
  老板给钱了 → 标记为"已结算"
```

### 场景 2：大学生兼职

```
小张在麦当劳兼职，时薪 ¥25/h。

每次下班：
  打开 App → 记工时
  选择项目"麦当劳兼职" → 自动识别为时薪模式
  填 18:00-22:00 → 自动算 4h
  点保存 → ✅ 完成，耗时 10 秒

月底：
  打开对账 → 看到"麦当劳兼职 5月 48h ¥1,200"
  跟工资条对一下 → 没问题 ✅
```

### 场景 3：工厂工人

```
小李在 xx工厂 做计件，¥6/件。

每天下班：
  打开 App → 记工时
  选择项目"xx工厂" → 自动识别为计件模式
  点 [+10] [+10] [+10] [+10] [+10] → 50件
  点保存 → ✅ 完成，耗时 5 秒
```
